jQuery(document).ready(function($) {

  // This is a sample jQuery file.  Create as many of these as you like. Gulp
  // will grab all files here and concat them to ./assets/dist/js/app.js &
  // ./assets/dist/js/app.min.js.  This theme is set to enqueue both files
  // depending on whether you've set define(WP_DEBUG, 'true'); in your
  // wp-config.php file.

});
